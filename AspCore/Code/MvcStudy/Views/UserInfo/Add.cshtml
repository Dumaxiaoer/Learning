@model MvcStudy.Models.Sys_User
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Add</title>
</head>
<body>
    <div> 
        @using (Html.BeginForm("Add", "UserInfo", FormMethod.Post)) {
            <span>姓名：</span>
            @Html.TextBoxFor(u => u.Account);
            <input type="submit" value="添加">            
        }
    </div>
</body>
</html>
